:status: done
task_id: osc-general-data-backend
pr_url: https://github.com/muraltactilsc/mt-dibujando/pull/15
build: passing
summary: |
  Implemented the OSC General Data backend slice: new `oscprofile` + 6 catalog tables seeded with
  representative data, `GET/POST /api/osc/general-data`, the `GetReadOnly`/`SendMessageRegister`
  pure functions, and 6 read-only catalog endpoints under `/api/catalogs`.
blockers: none
next_hint: |
  The catalog seed in `003_osc_general_data.sql` is representative placeholder data only — not
  production content — matching the status of the earlier registration quiz placeholder seed.
  `apps/api/db/types.ts` carries a registered budget exception because it is generated by
  `kysely-codegen`.

  Smoke checks exercised against the running API:
  - `GET /api/osc/general-data?userProfileId=45` returned defaults sourced from `userprofile`
    (`name`, `email`, `nationalRegistryNumber`, `countryId`) with `readOnly: false` and no
    `oscProfileId`.
  - `POST /api/osc/general-data` created `oscProfileId: 1` for a new user and returned
    `needsResubmission: false`.
  - Repeating the POST with `oscProfileId: 1` updated the existing row (`city` changed to
    "Guadalajara") instead of inserting a duplicate.
  - Missing `name` returned `400` with the exact Spanish message
    "El campo Nombre corto de la organización es requerido.".
  - Invalid `postalCode: "123"` returned `400` with the exact regex failure message
    "El formato del campo Código/Apartado Postal no es válido.".
  - A fixture `oscprofile` with `dynamicsoscstatusid = '206430000'` returned `readOnly: true`
    and a subsequent POST returned `403 { code: "osc_profile_locked" }`.
  - A fixture `oscprofile` with `dynamicsoscstatusid = '206430002'` allowed the update and
    returned `needsResubmission: true`.
